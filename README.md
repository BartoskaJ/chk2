# Checklist App

## Installation
1. Import `schema.sql` into MySQL.
2. Copy `config.php` and adjust database and app settings.
3. Set webroot to `public/`.
4. Cron: `* * * * * php /path/to/cron/scheduler.php`
